% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igraph.R
\name{phylopic_igraph}
\alias{phylopic_igraph}
\title{Use PhyloPic silhouettes with igraph}
\description{
When both \code{rphylopic} and \code{igraph} are loaded, rphylopic registers a
custom vertex shape called \code{"phylopic"}. Setting \code{vertex.shape = "phylopic"}
in \code{\link[igraph:plot.igraph]{igraph::plot.igraph()}} renders each vertex as a PhyloPic silhouette.
}
\details{
The shape accepts the following vertex parameters, mirroring the
arguments of \code{\link[=add_phylopic_base]{add_phylopic_base()}}:

\itemize{
\item \code{vertex.img}, \code{vertex.name}, \code{vertex.uuid} — silhouette source
\item \code{vertex.size} — silhouette height (in plot units)
\item \code{vertex.color}, \code{vertex.frame.color}, \code{vertex.alpha} — fill, outline,
opacity
\item \code{vertex.horizontal}, \code{vertex.vertical}, \code{vertex.angle} — orientation
\item \code{vertex.hjust}, \code{vertex.vjust} — anchoring
\item \code{vertex.remove_background}, \code{vertex.verbose}, \code{vertex.filter} —
passed to \code{\link[=add_phylopic_base]{add_phylopic_base()}}
\item \code{vertex.clip_scale} — numeric scale factor controlling the clipping
of the edges (default: \code{0.7})
}
}
\section{Note on interactive resizing}{

PhyloPic silhouettes are drawn as vector graphics on top of igraph's
base-graphics plot. Rendering as vectors preserves silhouette resolution;
however, when the graphics device is resized interactively the silhouettes
will not reposition along with the underlying graph layout. The graph nodes,
edges, and labels will redraw at their new coordinates while the
silhouettes remain anchored to their original device positions. To
restore alignment, re-run the \code{plot()} call after resizing.
}

\examples{
\dontrun{
library(igraph)
g <- make_ring(10)
plot(g, vertex.shape = "phylopic", vertex.name = "Gorilla",
     vertex.color = rainbow(vcount(g)))
}
}
\seealso{
\code{\link[=add_phylopic_base]{add_phylopic_base()}}, \code{\link[igraph:add_shape]{igraph::add_shape()}}
}
